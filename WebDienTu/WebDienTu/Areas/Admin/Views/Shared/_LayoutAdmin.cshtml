<!-- _LayoutAdmin.cshtml -->
@{
    ViewData["Title"] = "Trang Quản Trị";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin - @ViewData["Title"]</title>
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="~/images/icon/favicon.jpg" />
    <link rel="shortcut icon" type="image/png" href="~/favicon.png" />

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/cssAdmin/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/cssAdmin/navbarAdmin.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/cssAdmin/HeaderAdmin.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/cssAdmin/HomeAdmin.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
        /* Sidebar */
        #sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: 250px;
            background-color: #343a40;
            overflow: hidden;
            z-index: 1000;
            transition: width 0.3s ease;
        }

            #sidebar.collapsed {
                width: 60px;
            }

            #sidebar .nav-link i {
                width: 20px;
                margin-right: 10px;
                text-align: center;
                transition: margin 0.3s ease;
            }

            #sidebar.collapsed .nav-link i {
                margin-right: 0;
            }

            #sidebar .sidebar-text {
                transition: opacity 0.3s ease, transform 0.3s ease;
            }

            #sidebar.collapsed .sidebar-text {
                display: none;
                opacity: 0;
                transform: translateX(-10px);
            }

        /* Main content & footer */
        #mainContent, #footerAdmin {
            margin-left: 250px;
            transition: margin-left 0.3s ease;
        }

            #mainContent.collapsed, #footerAdmin.collapsed {
                margin-left: 60px;
            }

        /* Header */
        #headerAdmin {
            position: fixed;
            top: 0;
            left: 250px;
            width: calc(100% - 250px);
            z-index: 999;
            transition: left 0.3s ease, width 0.3s ease;
        }

            #headerAdmin.collapsed {
                left: 60px;
                width: calc(100% - 60px);
            }
    </style>
</head>
<body>
    <header id="headerAdmin">
        @await Html.PartialAsync("_Header")
    </header>
    @await Html.PartialAsync("_NavAdmin")
    <div id="mainContent" class="p-4 mt-5">
        @RenderBody()
    </div>

    <footer id="footerAdmin" class="bg-light text-center text-muted py-3 mt-4 border-top">
        @await Html.PartialAsync("_FooterAdmin")
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        window.addEventListener("DOMContentLoaded", function () {
            const sidebar = document.getElementById("sidebar");
            const toggleBtn = document.getElementById("toggleSidebar");
            const mainContent = document.getElementById("mainContent");
            const footer = document.getElementById("footerAdmin");
            const header = document.getElementById("headerAdmin");

            if (!sidebar || !toggleBtn) return;

            // Load trạng thái từ localStorage
            if (localStorage.getItem("sidebarState") === "collapsed") {
                sidebar.classList.add("collapsed");
                mainContent.classList.add("collapsed");
                footer.classList.add("collapsed");
                header.classList.add("collapsed");
            }

            toggleBtn.addEventListener("click", function () {
                sidebar.classList.toggle("collapsed");
                mainContent.classList.toggle("collapsed");
                footer.classList.toggle("collapsed");
                header.classList.toggle("collapsed");

                // Lưu trạng thái trên localStorage
                localStorage.setItem("sidebarState",
                    sidebar.classList.contains("collapsed") ? "collapsed" : "expanded"
                );
            });
        });
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>